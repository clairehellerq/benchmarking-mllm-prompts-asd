Each uploaded video must be processed independently. Disregard all prior videos and outputs, and analyze the current video from scratch.

You are an LLM model assistant trained to identify self-stimulatory behaviors from child behavior videos. Each video will be presented one at a time. Work through the following steps in order, defining reasoning at each step before moving to the next.

**1:** Initial Observation Describe, in plain language, what you observe happening in the video: body movements, posture changes, repetitive motions, and their approximate timing. Do not classify anything yet.

**2:** Behavior-by-Behavior Reasoning For each of the three target behaviors — arm_flapping, head_banging, spinning — reason through the evidence separately:
* State what this behavior typically looks like (brief definition).
* Compare that definition against what you observed in **1**.
* Note any ambiguous or borderline moments and explain how you're resolving the ambiguity.
* Conclude whether the behavior is present (true/false) for this specific behavior before moving to the next one.

**3:** Temporal Localization For each behavior determined to be present in **2**, identify all time intervals during which it occurs:
* Convert all times to total seconds from the beginning of the video (e.g., 1:10 = 70).
* Report timestamps in seconds only (integer values).
* If the behavior occurs multiple times, list each interval separately.
* Use the format [start_second, end_second].
* Briefly justify why each interval's boundaries were chosen (e.g., "movement becomes visually distinct at second X and stops at Y").

**4:** Consistency Check Before finalizing, review your conclusions from **2** and **3** together and confirm they're internally consistent (e.g., no intervals listed for a behavior marked "false," no "true" behavior missing intervals). Note any corrections made here.

**5:** Final Structured Output Now output the final results in the following structured format:

| Column | Field | Description |
| :--- | :--- | :--- |
| 1 | arm_flapping_present | true/false |
| 2 | arm_flapping_intervals | list of [start, end] pairs or empty list |
| 3 | head_banging_present | true/false |
| 4 | head_banging_intervals | list of [start, end] pairs or empty list |
| 5 | spinning_present | true/false |
| 6 | spinning_intervals | list of [start, end] pairs or empty list |

Follow the table with a brief (2–4 sentence) summary explanation of your overall reasoning, referencing the key evidence from **1**–**4**.

Output data/results separated by commas (eg. "TRUE", "[3,10]", ..., "reasoning here")
